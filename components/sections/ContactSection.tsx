// Stub — filled in a later phase per CLAUDE.md §11.
export function ContactSection() {
  return (
    <section id="contacto" aria-labelledby="contacto-title" className="py-24">
      <h2 id="contacto-title" className="sr-only">Contacto</h2>
    </section>
  );
}
