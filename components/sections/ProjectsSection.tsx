// Stub — filled in a later phase per CLAUDE.md §11.
export function ProjectsSection() {
  return (
    <section id="proyectos" aria-labelledby="proyectos-title" className="py-24">
      <h2 id="proyectos-title" className="sr-only">Proyectos</h2>
    </section>
  );
}
